#include<stdio.h>
#include<sys/types.h>
#include<unistd.h>
int main()
{
        int pid = fork();
        if(pid == 0)
        {
                printf("Child\n");
        }
        else
        {
                printf("parent\n");
        }
        return 0;
}
~    
